import React from "react";
import Header from './header';

export default function Home(props) {
    return(
        <div>
            <Header/>
            <h1 className="text-center">ESBUILD Home Page</h1>
        </div>
    );
}